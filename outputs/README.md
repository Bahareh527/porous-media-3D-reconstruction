# Outputs

Use this directory for generated artefacts such as:

- `checkpoints/` - model weights and saved models
- `figures/` - plots and visual comparisons
- `predictions/` - reconstructed or segmented volumes
- `metrics/` - CSV or JSON evaluation summaries

Generated outputs are ignored by Git by default. Publish only small, representative results with clear provenance and permission.

